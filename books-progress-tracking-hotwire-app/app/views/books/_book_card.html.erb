<div class="book">
  <%= link_to book do %>
    <div class="card">
      <img class="rounded" src="<%= book.image %>" alt="book image" />
      <div class="card-body">
        <p class="card-text"><%= book.title %></p>
      </div>
    </div>
  <% end %>
</div>
